[Unit]
Description=Pi Backup Manager
After=network.target

[Service]
Type=simple
User=pi
WorkingDirectory=/home/pi/pi-backup-manager
Environment="PI_BACKUP_HOST=0.0.0.0"
Environment="PI_BACKUP_PORT=8080"
ExecStart=/home/pi/pi-backup-manager/.venv/bin/uvicorn app.main:app --host ${PI_BACKUP_HOST} --port ${PI_BACKUP_PORT}
Restart=on-failure

[Install]
WantedBy=multi-user.target
